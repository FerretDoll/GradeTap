# 大模型自动批改作业网站项目架构设计文档

> 目标：从零开始开发一款面向教师的大模型自动批改作业网站。  
> 使用场景：教师上传作业要求、参考答案和学生作业，系统自动完成题目解析、分值设置、答案抽取、按题批改、成绩汇总和报告生成。  
> 开发建议：本文档可作为 Codex 从零生成项目代码的需求说明与架构参考。

---

## 1. 项目定位

本项目不是简单地“把学生作业丢给大模型批改”，而是构建一个完整、可控、可追踪、可复核的 AI 作业批改流水线。

核心定位：

> 面向高校与高职教师的 AI 作业批改助手，支持文档作业、代码作业、实验报告和课程项目的自动评分、反馈生成与班级学情分析。

核心价值：

1. 上传作业，自动拆题。
2. 自动生成评分标准，总分 100 分。
3. 按学生抽取每道题答案，避免串题。
4. 按题批量批改所有学生答案，保持评分一致。
5. 教师可复核、可修改、可重批。
6. 自动生成成绩表、学生反馈和班级分析报告。

---

## 2. 竞品参考：Swiftgrade 可学习点

参考网站：

> https://goswiftgrade.com/

Swiftgrade 的核心产品表达是：

> Grade handwritten tests in minutes - not hours.

对本项目的启发：

### 2.1 直接打教师痛点

不要一上来讲“大模型”“智能体”“RAG”“算法”，而是直接表达教师能获得什么。

推荐表达：

> 上传作业，自动拆题，统一评分，快速生成成绩表和讲评报告。

或：

> 让老师从重复批改中解放出来，把更多时间用于讲评与教学改进。

---

### 2.2 前端流程要简单

后端可以有复杂的批改流水线，但前端展示应尽量简化。

建议前端包装为 4 步：

1. 上传材料  
   上传作业要求、参考答案、学生作业。

2. 确认评分标准  
   AI 自动拆分题目并设置分值，教师可修改。

3. 自动批改  
   系统抽取学生答案，按题统一批改。

4. 查看结果  
   生成成绩表、学生反馈和班级分析报告。

---

### 2.3 AI 初批 + 教师复核

产品不应宣传成：

> AI 自动批改，老师不用管。

而应宣传成：

> AI 完成初批，教师掌握最终评分权。

建议系统支持：

1. 教师确认评分标准。
2. 教师查看答案抽取结果。
3. 教师修改单题得分。
4. 教师批量调整同类答案得分。
5. 教师确认后再发布成绩。

---

### 2.4 按题查看所有学生答案

Swiftgrade 的重要思路是：

> Review all responses for a single question.

这与本项目的 `grade_by_question` 模块高度契合。

建议前端提供“按题批改视图”：

| 学生 | 学生答案 | AI 得分 | AI 评语 | 教师操作 |
|---|---|---:|---|---|
| 张三 | 第1题答案 | 8/10 | 思路正确，格式不规范 | 修改 |
| 李四 | 第1题答案 | 6/10 | 缺少关键步骤 | 修改 |
| 王五 | 第1题答案 | 10/10 | 完全正确 | 修改 |

---

### 2.5 增加相似答案分组

Swiftgrade 有 Group Grading 的思路，即把相似答案分组，教师可以批量编辑分数和反馈。

本项目可增加：

```text
group_answers
```

作用：

> 同一道题下，把相似答案聚类成几个组，教师只需要对同类答案确认一次分数和反馈。

示例：

```text
第1题：
A组：完全正确，18人
B组：缺少 WHERE 条件，10人
C组：字段名错误，7人
D组：语法错误，6人
E组：未作答，4人
```

---

## 3. 总体架构

推荐采用前后端分离架构。

```text
前端 Web
 ├── 文件上传
 ├── 批改任务配置
 ├── 批改进度展示
 ├── 中间结果查看
 ├── 成绩表查看
 └── 报告导出

后端 API 服务
 ├── 用户与班级管理
 ├── 文件管理服务
 ├── 批改任务服务
 ├── LLM 调用服务
 ├── 成绩管理服务
 └── 报告生成服务

异步任务队列
 ├── parse_files
 ├── set_score
 ├── prepare_students
 ├── extract_answers
 ├── group_answers
 ├── grade_by_question
 ├── teacher_review
 └── summary

数据存储
 ├── MySQL / PostgreSQL：结构化数据
 ├── Redis：任务状态、缓存、队列
 ├── MinIO / OSS：原始文件、报告文件
 └── 向量库，可选：历史评分标准、优秀答案案例
```

---

## 4. 推荐技术选型

### 4.1 前端

推荐：

```text
Vue 3 + Element Plus + Axios + Pinia
```

说明：

- Vue 3：前端开发框架。
- Element Plus：Vue 3 的 UI 组件库，适合后台管理系统。
- Axios：请求后端 API。
- Pinia：前端状态管理。

适合使用 Element Plus 的页面组件：

| 页面功能 | Element Plus 组件 |
|---|---|
| 上传作业文件 | Upload |
| 展示学生列表 | Table |
| 编辑评分标准 | Form |
| 显示批改进度 | Steps |
| 查看批改详情 | Dialog / Drawer |
| 成绩分页查看 | Pagination |
| 任务状态提示 | Tag / Progress |
| 批量操作 | Button / Checkbox |

---

### 4.2 后端

推荐：

```text
Python FastAPI
```

原因：

1. Python 对文档解析、大模型调用、数据处理更友好。
2. FastAPI 开发接口效率高。
3. 方便接入 OpenAI、DeepSeek、通义千问、智谱等模型服务。
4. 方便使用 python-docx、PyMuPDF、pdfplumber、pandas 等工具。

---

### 4.3 异步任务

推荐：

```text
Celery + Redis
```

原因：

批改任务属于长任务，不建议直接在 HTTP 请求里同步完成。

Celery 负责执行批改流水线，Redis 用于任务队列和状态缓存。

---

### 4.4 数据库和文件存储

推荐：

```text
PostgreSQL / MySQL：业务数据
Redis：任务队列、任务进度、缓存
MinIO：文件存储
```

第一阶段也可以用本地文件系统替代 MinIO，但建议代码结构预留对象存储接口。

---

## 5. 后端目录结构建议

```text
backend/
├── app/
│   ├── main.py
│   ├── api/
│   │   ├── task_api.py
│   │   ├── file_api.py
│   │   ├── grading_api.py
│   │   └── report_api.py
│   ├── services/
│   │   ├── file_parse_service.py
│   │   ├── score_service.py
│   │   ├── student_service.py
│   │   ├── answer_extract_service.py
│   │   ├── answer_group_service.py
│   │   ├── grading_service.py
│   │   ├── teacher_review_service.py
│   │   ├── summary_service.py
│   │   └── llm_service.py
│   ├── tasks/
│   │   ├── grading_pipeline.py
│   │   └── celery_tasks.py
│   ├── models/
│   │   ├── task.py
│   │   ├── file.py
│   │   ├── question.py
│   │   ├── student.py
│   │   ├── answer.py
│   │   ├── answer_group.py
│   │   └── grading_result.py
│   ├── schemas/
│   ├── prompts/
│   │   ├── set_score_prompt.py
│   │   ├── extract_answer_prompt.py
│   │   ├── group_answer_prompt.py
│   │   └── grade_question_prompt.py
│   └── utils/
│       ├── docx_parser.py
│       ├── pdf_parser.py
│       ├── excel_export.py
│       ├── report_export.py
│       └── text_cleaner.py
```

---

## 6. 核心业务流程

### 6.1 批改流水线

```text
新建批改任务
   ↓
上传作业要求文件、参考答案文件、学生作业文件
   ↓
启动批改流水线
   ↓
parse_files
   ↓
set_score
   ↓
教师确认/修改分值标准
   ↓
prepare_students
   ↓
extract_answers
   ↓
教师查看答案抽取结果
   ↓
group_answers，可选
   ↓
grade_by_question
   ↓
teacher_review
   ↓
summary
   ↓
生成成绩表与批改报告
```

---

### 6.2 推荐前端展示流程

```text
第1步：上传材料
    ↓
第2步：确认评分标准
    ↓
第3步：自动批改
    ↓
第4步：查看结果
```

---

## 7. 核心模块设计

---

## 7.1 parse_files

### 功能

读取并解析作业要求文件和参考答案文件。

### 输入

```json
{
  "task_id": "T202605090001",
  "requirement_file_id": "file_001",
  "reference_file_id": "file_002"
}
```

### 输出

```json
{
  "task_id": "T202605090001",
  "requirement_text": "作业要求全文...",
  "reference_text": "参考答案全文..."
}
```

### 支持文件类型

MVP 阶段建议支持：

```text
.docx
.pdf
.txt
.md
```

后续支持：

```text
.xlsx
.pptx
图片 OCR
压缩包 zip
```

### 实现建议

```text
.docx：python-docx
.pdf：pymupdf / pdfplumber
.txt：直接读取
图片：后续接 OCR
```

### 文本清洗要求

1. 去除多余空行。
2. 修正常见乱码。
3. 保留题号结构。
4. 保留表格文本。
5. 保留代码缩进。

### 推荐数据对象

```python
class ParsedFile:
    file_id: str
    file_type: str
    raw_text: str
    clean_text: str
    page_blocks: list
```

---

## 7.2 set_score

### 功能

调用 LLM，根据题目内容设置每题分值，总分 100 分。

### 输入

```json
{
  "requirement_text": "作业要求文本",
  "grading_instruction": "请重点关注 SQL 语法正确性、查询结果准确性和代码规范。"
}
```

### 输出

```json
[
  {
    "question_no": "1",
    "question_title": "数据库表结构设计",
    "question_content": "请设计学生信息表...",
    "score": 20,
    "grading_points": [
      {
        "point": "字段设计完整",
        "score": 8
      },
      {
        "point": "主键设置合理",
        "score": 5
      },
      {
        "point": "数据类型选择正确",
        "score": 5
      },
      {
        "point": "命名规范",
        "score": 2
      }
    ]
  }
]
```

### 关键要求

不只是输出“题号 + 分值”，还要输出“评分细则”。

理由：

1. 批改更稳定。
2. 教师可以调整。
3. 学生反馈更具体。
4. 出现争议时更容易解释。

### 推荐流程

```text
LLM 生成初始评分标准
    ↓
系统检查总分是否为 100
    ↓
如果不是 100，自动修正或要求 LLM 修正
    ↓
教师确认或修改
    ↓
锁定评分标准
```

---

## 7.3 prepare_students

### 功能

读取所有学生作业文件，提取学生姓名、学号和作业内容。

### 输入

```json
{
  "task_id": "T202605090001",
  "student_file_ids": ["file_101", "file_102", "file_103"]
}
```

### 输出

```json
[
  {
    "student_id": "20240101",
    "student_name": "张三",
    "file_id": "file_101",
    "content": "学生作业全文..."
  }
]
```

### 学生信息提取策略

建议三层提取：

1. 从文件名提取  
   例如：`20240101_张三_数据库作业.docx`

2. 从正文开头提取  
   例如：  
   `姓名：张三`  
   `学号：20240101`

3. LLM 辅助识别  
   当规则无法识别时，调用 LLM 提取。

### 推荐文件名规范

```text
学号_姓名_作业名称.docx
```

例如：

```text
20240101_张三_SQL作业.docx
20240102_李四_SQL作业.docx
```

### 推荐数据对象

```python
class StudentSubmission:
    id: str
    task_id: str
    student_no: str
    student_name: str
    file_id: str
    raw_text: str
    clean_text: str
    parse_status: str
```

---

## 7.4 extract_answers

### 功能

从学生作业中提取每个题目的答案。

这是本系统的关键模块之一。

### 输入

```json
{
  "students": [
    {
      "student_no": "20240101",
      "student_name": "张三",
      "content": "学生作业全文..."
    }
  ],
  "questions": [
    {
      "question_no": "1",
      "question_content": "请完成数据库设计..."
    }
  ]
}
```

### 输出

```json
[
  {
    "student_no": "20240101",
    "student_name": "张三",
    "answers": {
      "1": {
        "answer_text": "学生第1题答案...",
        "confidence": 0.93,
        "status": "matched"
      },
      "2": {
        "answer_text": "学生第2题答案...",
        "confidence": 0.88,
        "status": "matched"
      },
      "3": {
        "answer_text": "",
        "confidence": 0.20,
        "status": "missing"
      }
    }
  }
]
```

### 推荐策略

不要完全依赖 LLM。

建议采用：

```text
规则解析优先 + LLM 修正兜底
```

### 规则匹配题号

识别常见题号格式：

```text
1.
1、
一、
第1题
题目1
（1）
Question 1
Q1
```

### 按题号切片

根据题号位置把学生答案切成多个片段。

### 与标准题目对齐

如果学生写的是“第一题、第二题、第三题”，则按标准题目顺序对齐。

### LLM 兜底场景

以下情况调用 LLM：

1. 学生没有明确写题号。
2. 学生题号顺序混乱。
3. 学生把多个答案写在一起。
4. 学生跳题。
5. 答案中包含大量截图说明或代码。

### 输出状态

每道题建议带状态：

```text
matched：成功匹配
missing：疑似未作答
ambiguous：匹配不确定
manual_check：建议人工确认
```

---

## 7.5 group_answers

### 功能

对同一道题下的学生答案进行相似性分组。

该模块不是 MVP 必需，但非常适合作为产品亮点。

### 输入

```json
{
  "question_no": "1",
  "student_answers": [
    {
      "student_no": "20240101",
      "student_name": "张三",
      "answer_text": "SELECT * FROM student WHERE age > 18;"
    },
    {
      "student_no": "20240102",
      "student_name": "李四",
      "answer_text": "SELECT * FROM student;"
    }
  ]
}
```

### 输出

```json
{
  "question_no": "1",
  "groups": [
    {
      "group_id": "G1",
      "group_name": "基本正确",
      "summary": "包含正确查询条件，语法基本无误",
      "student_count": 18,
      "students": ["20240101", "20240105"]
    },
    {
      "group_id": "G2",
      "group_name": "缺少筛选条件",
      "summary": "SQL 语句能运行，但缺少 WHERE 条件",
      "student_count": 10,
      "students": ["20240102", "20240107"]
    }
  ]
}
```

### 实现建议

第一阶段可以用 LLM 进行分组。

后期可以使用：

1. 文本向量 Embedding。
2. 相似度聚类。
3. LLM 生成组名与错误类型。

---

## 7.6 grade_by_question

### 功能

按题批量批改所有学生答案。

推荐方案：

> 对每道题，一次性传入该题所有学生的答案，确保评分标准统一。

### 优点

1. 同一道题评分标准更统一。
2. 更容易发现不同学生之间的答案差异。
3. 可以减少评分漂移。
4. 出错时只需要重批某一道题。
5. 方便教师查看某题全班掌握情况。

### 输入

```json
{
  "question_no": "1",
  "question_content": "请设计学生信息表，包括学号、姓名、性别、年龄、专业等字段。",
  "reference_answer": "CREATE TABLE student (...)",
  "score": 20,
  "grading_points": [
    {
      "point": "字段完整",
      "score": 6
    },
    {
      "point": "主键设置正确",
      "score": 4
    },
    {
      "point": "数据类型合理",
      "score": 5
    },
    {
      "point": "SQL语法正确",
      "score": 3
    },
    {
      "point": "命名规范",
      "score": 2
    }
  ],
  "student_answers": [
    {
      "student_no": "20240101",
      "student_name": "张三",
      "answer_text": "CREATE TABLE student (...);"
    },
    {
      "student_no": "20240102",
      "student_name": "李四",
      "answer_text": "CREATE TABLE stu (...);"
    }
  ]
}
```

### 输出

```json
{
  "question_no": "1",
  "results": [
    {
      "student_no": "20240101",
      "student_name": "张三",
      "score": 18,
      "max_score": 20,
      "deductions": [
        {
          "point": "命名规范",
          "deduct": 2,
          "reason": "字段命名不够统一"
        }
      ],
      "comment": "整体完成较好，表结构完整，主键设置正确。"
    },
    {
      "student_no": "20240102",
      "student_name": "李四",
      "score": 14,
      "max_score": 20,
      "deductions": [
        {
          "point": "字段完整",
          "deduct": 3,
          "reason": "缺少专业字段"
        }
      ],
      "comment": "基本完成建表任务，但字段完整性需要改进。"
    }
  ]
}
```

### 分批策略

不要一次性传入太多学生答案。

建议：

```text
每题学生数 <= 20：一次性批改
每题学生数 > 20：按 20 人一组批改
```

例如 60 人班级：

```text
第1题第1批：1-20号学生
第1题第2批：21-40号学生
第1题第3批：41-60号学生
```

每批都应重复完整评分标准。

---

## 7.7 teacher_review

### 功能

教师复核 AI 批改结果。

### 支持操作

1. 修改某个学生某道题得分。
2. 修改某个学生某道题评语。
3. 批量修改相似答案组得分。
4. 标记需要人工处理。
5. 确认最终成绩。
6. 对某一道题重新批改。
7. 对某个学生重新批改。

### 推荐视图

#### 按学生查看

| 学号 | 姓名 | 第1题 | 第2题 | 第3题 | 总分 | 操作 |
|---|---|---:|---:|---:|---:|---|
| 20240101 | 张三 | 18 | 25 | 45 | 88 | 查看详情 |

#### 按题查看

| 题号 | 平均分 | 满分人数 | 低分人数 | 主要问题 |
|---|---:|---:|---:|---|
| 1 | 16.8 | 8 | 5 | 主键设置不规范 |

#### 按状态筛选

```text
完全正确
部分正确
错误
未作答
需要人工复核
答案抽取异常
```

---

## 7.8 summary

### 功能

生成完整的批改汇总报告。

### 输入

```json
{
  "grading_results": [],
  "score_standards": []
}
```

### 输出

```json
{
  "class_statistics": {
    "student_count": 45,
    "average_score": 82.5,
    "max_score": 98,
    "min_score": 51,
    "pass_rate": 0.91,
    "excellent_rate": 0.28
  },
  "question_statistics": [
    {
      "question_no": "1",
      "average_score": 16.8,
      "max_score": 20,
      "difficulty": "中等",
      "common_problems": [
        "部分学生主键设置不规范",
        "字段类型选择不合理"
      ]
    }
  ],
  "teaching_suggestions": [
    "建议加强数据库字段类型设计训练",
    "建议通过案例讲解主键与约束的使用"
  ]
}
```

### summary 应包含

1. 班级平均分。
2. 最高分、最低分。
3. 各题平均得分率。
4. 各题失分人数。
5. 高频错误类型。
6. 需要重点辅导学生。
7. 需要课堂讲解的知识点。
8. 下一节课教学建议。
9. 可选：生成课堂讲评大纲。

---

## 8. LLM 调用策略

### 8.1 统一封装 LLM 服务

所有模型调用都通过 `llm_service.py` 完成，不要在各业务模块中直接调用模型 API。

推荐接口：

```python
class LLMService:
    def chat_json(self, prompt: str, schema: dict | None = None) -> dict:
        pass

    def chat_text(self, prompt: str) -> str:
        pass
```

### 8.2 JSON 输出要求

所有需要入库的 LLM 输出，必须要求模型只输出 JSON。

Prompt 中明确：

```text
你必须只输出合法 JSON，不要输出 Markdown，不要输出解释文字。
```

### 8.3 JSON 校验和重试

建议机制：

```text
第一次：正常调用
第二次：要求模型修复 JSON 格式
第三次：标记为人工复核
```

### 8.4 评分锚点

为了让不同批次评分更统一，在 `grade_by_question` prompt 中加入评分锚点：

```text
满分答案特征：
- 完整覆盖所有要求
- 无明显语法错误
- 表达清晰规范

中等答案特征：
- 完成主要任务
- 存在部分缺漏
- 不影响整体理解

低分答案特征：
- 答案不完整
- 与题目要求明显不匹配
- 存在严重错误
```

对于 SQL / 编程作业，可增加：

```text
语法正确性
逻辑正确性
结果完整性
规范性
可运行性
```

---

## 9. Prompt 模板

### 9.1 set_score Prompt

```text
你是一名课程作业评分标准设计专家。

请根据作业要求识别所有题目，并为每道题设置分值。
总分必须为100分。
每道题需要包含题号、题目内容、分值和评分细则。

要求：
1. 总分必须严格等于100分。
2. 每道题必须有明确题号。
3. 每道题至少包含2个评分细则。
4. 评分细则分值之和必须等于该题总分。
5. 请只输出合法 JSON，不要输出 Markdown，不要输出解释文字。

输出格式：
{
  "questions": [
    {
      "question_no": "1",
      "question_content": "...",
      "score": 20,
      "rubrics": [
        {
          "item": "功能完整性",
          "score": 8
        }
      ]
    }
  ]
}

作业要求：
{{requirement_text}}

批改说明：
{{grading_instruction}}
```

---

### 9.2 extract_answers Prompt

```text
你是一名作业答案结构化提取助手。

请根据标准题目列表，从学生作业中提取每道题对应的答案。
不要批改，不要评价，只做答案抽取。

要求：
1. 答案必须和题号强绑定。
2. 如果某题没有找到答案，answer_text 置为空，status 标记为 missing。
3. 如果不确定，status 标记为 ambiguous。
4. 如果成功匹配，status 标记为 matched。
5. confidence 是 0 到 1 之间的小数。
6. 请只输出合法 JSON，不要输出 Markdown，不要输出解释文字。

输出格式：
{
  "student_no": "20240101",
  "student_name": "张三",
  "answers": [
    {
      "question_no": "1",
      "answer_text": "...",
      "status": "matched",
      "confidence": 0.92
    }
  ]
}

标准题目列表：
{{questions}}

学生信息：
{{student_info}}

学生作业全文：
{{student_content}}
```

---

### 9.3 group_answers Prompt

```text
你是一名作业答案聚类分析助手。

现在只分析同一道题的所有学生答案。
请根据答案的相似性、错误类型和完成程度，将学生答案分成若干组。

要求：
1. 同一组内的答案应具有相似特征。
2. 每组需要给出组名、组内答案特征、可能得分区间。
3. 不要直接给最终分数，只做分组。
4. 每个学生必须且只能出现在一个组中。
5. 请只输出合法 JSON，不要输出 Markdown，不要输出解释文字。

输出格式：
{
  "question_no": "1",
  "groups": [
    {
      "group_name": "基本正确",
      "summary": "答案完整，逻辑正确，仅有少量格式问题",
      "suggested_score_range": "18-20",
      "students": [
        {
          "student_no": "20240101",
          "student_name": "张三"
        }
      ]
    }
  ]
}

题目：
{{question}}

学生答案：
{{student_answers}}
```

---

### 9.4 grade_by_question Prompt

```text
你是一名严格、公正、稳定的课程作业批改教师。

现在只批改同一道题。
请根据题目要求、参考答案和评分细则，对所有学生答案进行评分。
所有学生必须使用同一评分标准。

要求：
1. 不得因为表达方式不同而随意扣分。
2. 不得给出超过本题满分的分数。
3. 如果答案为空或明显未作答，得分为0。
4. 必须给出扣分点和简短评语。
5. 分数可以是整数，也可以保留0.5分。
6. 请只输出合法 JSON，不要输出 Markdown，不要输出解释文字。

评分锚点：
满分答案特征：
- 完整覆盖所有要求
- 无明显语法错误
- 表达清晰规范

中等答案特征：
- 完成主要任务
- 存在部分缺漏
- 不影响整体理解

低分答案特征：
- 答案不完整
- 与题目要求明显不匹配
- 存在严重错误

输出格式：
{
  "question_no": "1",
  "results": [
    {
      "student_no": "20240101",
      "student_name": "张三",
      "score": 18,
      "max_score": 20,
      "deductions": [
        {
          "rubric_item": "命名规范",
          "deduct_score": 2,
          "reason": "字段命名不统一"
        }
      ],
      "comment": "整体完成较好。"
    }
  ]
}

题目：
{{question}}

参考答案：
{{reference_answer}}

评分细则：
{{rubrics}}

学生答案：
{{student_answers}}
```

---

## 10. 数据库设计

### 10.1 grading_task

```sql
CREATE TABLE grading_task (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_name VARCHAR(255),
    course_name VARCHAR(255),
    class_name VARCHAR(255),
    teacher_id BIGINT,
    status VARCHAR(50),
    grading_instruction TEXT,
    created_at DATETIME,
    updated_at DATETIME
);
```

任务状态：

```text
created
files_uploaded
parsed
score_generated
score_confirmed
students_prepared
answers_extracted
answers_grouped
grading
graded
reviewing
reviewed
summary_generated
failed
```

---

### 10.2 uploaded_file

```sql
CREATE TABLE uploaded_file (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_id BIGINT,
    file_name VARCHAR(255),
    file_type VARCHAR(50),
    file_role VARCHAR(50),
    storage_path VARCHAR(500),
    parse_status VARCHAR(50),
    raw_text LONGTEXT,
    clean_text LONGTEXT,
    created_at DATETIME
);
```

file_role 可取：

```text
requirement
reference_answer
student_submission
report
```

---

### 10.3 question

```sql
CREATE TABLE question (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_id BIGINT,
    question_no VARCHAR(50),
    question_content TEXT,
    reference_answer LONGTEXT,
    total_score DECIMAL(5,2),
    sort_order INT,
    created_at DATETIME
);
```

---

### 10.4 question_rubric

```sql
CREATE TABLE question_rubric (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    question_id BIGINT,
    rubric_item TEXT,
    score DECIMAL(5,2),
    sort_order INT
);
```

---

### 10.5 student_submission

```sql
CREATE TABLE student_submission (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_id BIGINT,
    student_no VARCHAR(100),
    student_name VARCHAR(100),
    file_id BIGINT,
    content LONGTEXT,
    parse_status VARCHAR(50),
    created_at DATETIME
);
```

---

### 10.6 student_answer

```sql
CREATE TABLE student_answer (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_id BIGINT,
    student_submission_id BIGINT,
    question_id BIGINT,
    question_no VARCHAR(50),
    answer_text LONGTEXT,
    extract_status VARCHAR(50),
    confidence DECIMAL(5,4),
    created_at DATETIME
);
```

---

### 10.7 answer_group

```sql
CREATE TABLE answer_group (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_id BIGINT,
    question_id BIGINT,
    group_name VARCHAR(255),
    group_summary TEXT,
    suggested_score_range VARCHAR(50),
    created_at DATETIME
);
```

---

### 10.8 answer_group_member

```sql
CREATE TABLE answer_group_member (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    group_id BIGINT,
    student_answer_id BIGINT,
    created_at DATETIME
);
```

---

### 10.9 grading_result

```sql
CREATE TABLE grading_result (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    task_id BIGINT,
    student_submission_id BIGINT,
    question_id BIGINT,
    score DECIMAL(5,2),
    max_score DECIMAL(5,2),
    comment TEXT,
    raw_llm_output LONGTEXT,
    grading_status VARCHAR(50),
    review_status VARCHAR(50),
    created_at DATETIME,
    updated_at DATETIME
);
```

grading_status 可取：

```text
correct
partial
incorrect
missing
need_review
extract_error
```

review_status 可取：

```text
ai_generated
teacher_modified
teacher_confirmed
```

---

### 10.10 grading_deduction

```sql
CREATE TABLE grading_deduction (
    id BIGINT PRIMARY KEY AUTO_INCREMENT,
    grading_result_id BIGINT,
    rubric_item TEXT,
    deduct_score DECIMAL(5,2),
    reason TEXT
);
```

---

## 11. API 接口设计

### 11.1 创建批改任务

```http
POST /api/tasks
```

请求：

```json
{
  "task_name": "SQL数据库第13周作业",
  "course_name": "数据库应用技术",
  "class_name": "软件2301",
  "grading_instruction": "重点考查SQL语法、结果正确性和代码规范。"
}
```

---

### 11.2 上传文件

```http
POST /api/tasks/{task_id}/files
```

文件类型：

```text
requirement
reference_answer
student_submission
```

---

### 11.3 解析作业要求和参考答案

```http
POST /api/tasks/{task_id}/parse-files
```

---

### 11.4 生成分值标准

```http
POST /api/tasks/{task_id}/set-score
```

---

### 11.5 获取分值标准

```http
GET /api/tasks/{task_id}/questions
```

---

### 11.6 教师确认或修改分值标准

```http
PUT /api/tasks/{task_id}/questions
```

---

### 11.7 准备学生作业

```http
POST /api/tasks/{task_id}/prepare-students
```

---

### 11.8 抽取学生答案

```http
POST /api/tasks/{task_id}/extract-answers
```

---

### 11.9 相似答案分组

```http
POST /api/tasks/{task_id}/group-answers
```

---

### 11.10 按题批改

```http
POST /api/tasks/{task_id}/grade-by-question
```

---

### 11.11 重批某一道题

```http
POST /api/tasks/{task_id}/questions/{question_id}/regrade
```

---

### 11.12 教师修改批改结果

```http
PUT /api/tasks/{task_id}/grading-results/{result_id}
```

---

### 11.13 教师确认最终成绩

```http
POST /api/tasks/{task_id}/confirm-results
```

---

### 11.14 生成汇总报告

```http
POST /api/tasks/{task_id}/summary
```

---

### 11.15 导出成绩

```http
GET /api/tasks/{task_id}/export/excel
```

---

### 11.16 导出报告

```http
GET /api/tasks/{task_id}/export/report
```

---

## 12. 前端页面设计

### 12.1 登录页

基础登录即可，MVP 阶段可先用单教师模式。

---

### 12.2 批改任务列表页

字段：

| 任务名称 | 课程 | 班级 | 状态 | 创建时间 | 操作 |
|---|---|---|---|---|---|

---

### 12.3 新建批改任务页

教师填写：

1. 作业名称。
2. 课程名称。
3. 班级名称。
4. 批改说明。
5. 总分，默认 100 分。
6. 是否开启严格模式。

---

### 12.4 文件上传页

分三个区域：

1. 作业要求文件。
2. 参考答案文件。
3. 学生作业文件，可多选或上传压缩包。

上传后显示：

1. 文件名。
2. 文件类型。
3. 解析状态。
4. 学生姓名。
5. 学号。
6. 异常提示。

---

### 12.5 批改流程页

使用步骤条展示：

```text
1. 文件解析
2. 分值设定
3. 学生作业解析
4. 答案抽取
5. 相似答案分组
6. 按题批改
7. 教师复核
8. 汇总报告
```

---

### 12.6 分值标准确认页

| 题号 | 题目内容 | 分值 | 评分细则 | 操作 |
|---|---|---:|---|---|
| 1 | 数据库设计 | 20 | 字段完整6分，主键4分... | 编辑 |

---

### 12.7 学生答案抽取结果页

支持按学生查看和按题查看。

#### 按学生查看

| 学号 | 姓名 | 第1题 | 第2题 | 第3题 | 状态 |
|---|---|---|---|---|---|
| 20240101 | 张三 | 已提取 | 已提取 | 缺失 | 异常 |

#### 按题查看

| 题号 | 已提取人数 | 缺失人数 | 异常人数 | 操作 |
|---|---:|---:|---:|---|
| 1 | 45 | 0 | 2 | 查看 |

---

### 12.8 相似答案分组页

| 题号 | 分组名称 | 人数 | 主要特征 | 建议分数区间 | 操作 |
|---|---|---:|---|---|---|
| 1 | 基本正确 | 18 | SQL完整，逻辑正确 | 18-20 | 查看 |
| 1 | 缺少条件 | 10 | 缺少 WHERE | 12-16 | 查看 |

---

### 12.9 批改结果页

#### 按学生展示

| 学号 | 姓名 | 第1题 | 第2题 | 第3题 | 总分 | 操作 |
|---|---|---:|---:|---:|---:|---|
| 20240101 | 张三 | 18 | 25 | 45 | 88 | 查看详情 |

#### 按题展示

| 题号 | 平均分 | 满分人数 | 低分人数 | 主要问题 |
|---|---:|---:|---:|---|
| 1 | 16.8 | 8 | 5 | 主键设置不规范 |

---

### 12.10 报告页

展示：

1. 班级整体情况。
2. 分数分布。
3. 各题得分情况。
4. 高频错误。
5. 重点学生。
6. 教学建议。
7. 导出 Excel。
8. 导出报告。

---

## 13. 关键风险与解决方案

### 13.1 LLM 批改不稳定

解决方案：

1. 按题批改，减少评分漂移。
2. 使用固定评分细则。
3. 输出 JSON，便于校验。
4. 支持重批某题。
5. 保留 raw_llm_output。
6. 设置教师复核入口。

---

### 13.2 学生答案抽取错位

解决方案：

1. 规则抽取优先。
2. LLM 兜底。
3. 每题输出 confidence。
4. 前端标记 ambiguous。
5. 支持人工修正答案映射。

---

### 13.3 学生人数太多导致上下文过长

解决方案：

1. 按题批改。
2. 每题再按学生分批。
3. 每批控制在 10 到 20 人。
4. 评分标准每批固定。
5. 对异常结果单独重试。

---

### 13.4 参考答案不完整

解决方案：

1. 允许教师补充批改说明。
2. 允许教师编辑评分细则。
3. LLM 批改时同时参考题目、答案、评分点。
4. 对开放题设置“参考答案仅作参考”。

---

### 13.5 代码类作业只靠 LLM 不可靠

如果是 SQL、Python、Java 等代码类作业，后期建议增加自动运行验证。

例如 SQL 作业：

1. 准备测试数据库。
2. 执行学生 SQL。
3. 比较查询结果。
4. 将执行结果提供给 LLM。
5. LLM 结合运行结果评分。

---

## 14. 推荐开发阶段

### 14.1 第一阶段：MVP 版本

目标：跑通核心闭环。

功能：

1. 教师上传作业要求、参考答案、学生作业。
2. 解析 docx、pdf、txt。
3. LLM 自动生成题目与分值。
4. 教师确认评分标准。
5. 提取学生答案。
6. 按题批改。
7. 生成成绩表。
8. 导出 Excel。

---

### 14.2 第二阶段：增强版本

功能：

1. 批改报告生成。
2. 班级统计分析。
3. 教师人工复核。
4. 单题重批。
5. 异常答案提示。
6. 学生个性化反馈。
7. 相似答案分组。

---

### 14.3 第三阶段：专业版本

功能：

1. SQL / Python / Java 自动运行验证。
2. 查重检测。
3. 历史评分标准库。
4. 优秀答案库。
5. 课程知识点掌握分析。
6. 学情画像。
7. 教学改进建议。
8. 课堂讲评 PPT 大纲生成。

---

## 15. Codex 开发提示词建议

后续可以把下面这段直接交给 Codex 作为初始开发指令。

```text
请根据当前 Markdown 文档，从零开始开发一个“大模型自动批改作业网站”。

技术栈要求：
1. 前端使用 Vue 3 + Element Plus + Pinia + Axios。
2. 后端使用 Python FastAPI。
3. 数据库使用 MySQL 或 PostgreSQL。
4. 异步任务使用 Celery + Redis。
5. 文件存储第一阶段可使用本地存储，代码结构预留 MinIO 接口。
6. LLM 调用统一封装在 llm_service.py 中。

第一阶段只实现 MVP：
1. 创建批改任务。
2. 上传作业要求文件、参考答案文件和学生作业文件。
3. 解析 docx、pdf、txt 文件。
4. 调用 LLM 生成题目、分值和评分细则。
5. 教师确认评分标准。
6. 解析学生姓名、学号和作业内容。
7. 从学生作业中按题抽取答案。
8. 按题批量批改所有学生答案。
9. 生成学生总分表。
10. 导出 Excel。

开发要求：
1. 后端接口要符合文档中的 API 设计。
2. 数据库表结构参考文档中的 SQL。
3. 所有 LLM 输出必须要求 JSON 格式。
4. 所有关键中间结果必须入库。
5. 前端要提供任务流程步骤条。
6. 前端要支持查看分值标准、答案抽取结果和批改结果。
7. 代码结构要清晰，便于后续扩展 group_answers、teacher_review 和 summary。
```

---

## 16. 最终架构结论

本项目推荐采用：

```text
前端：Vue 3 + Element Plus
后端：FastAPI
数据库：PostgreSQL 或 MySQL
任务队列：Celery + Redis
文件存储：MinIO 或本地文件系统
LLM 服务：统一封装 llm_service
批改方式：按题批量批改
中间结果：全部入库，可查看、可修正、可重跑
```

最重要的设计原则：

> 不要让大模型一次性完成所有事情，而是把批改拆成“解析 → 设分 → 抽题 → 分组 → 批改 → 复核 → 汇总”的可控流水线。

重点模块：

1. `extract_answers`
2. `grade_by_question`
3. `group_answers`
4. `teacher_review`
5. `summary`

其中 MVP 阶段优先实现：

1. `parse_files`
2. `set_score`
3. `prepare_students`
4. `extract_answers`
5. `grade_by_question`
6. `export_excel`

后续再逐步增强为完整教师作业批改平台。
